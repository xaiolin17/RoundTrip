"""gold_agent.quant —— 商用级量化内核。

设计原则（对应商业化要求）：
1. **无前视（no look-ahead）**：所有特征在 t 时刻只使用 ≤t 的信息；标签用未来信息但严格隔离。
2. **点内（point-in-time）**：数据带时间戳，回放严格按时间推进。
3. **可复现**：随机种子固定，结果落盘。
4. **可检验**：每个模型输出逐时点分数序列，供统一评估器做 IC / 净收益 / 多重检验。
5. **可组合**：模型间通过标准接口 `ModelOutput` 交互，权重由数据决定而非人工拍脑袋。
"""
