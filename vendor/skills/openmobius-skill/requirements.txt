# OpenMobius-skill — runtime dependencies

# RAG retrieval (vector index + embedding)
chromadb>=0.5
# v5.3+ uses transformers' built-in Nomic text implementation, allowing the
# runtime to keep trust_remote_code disabled.
sentence-transformers>=5.3,<6
transformers>=5.5,<6
numpy>=1.24
einops>=0.7        # nomic-embed-text-v1.5 dep

# Chart annotation (PIL on user-uploaded images)
Pillow>=10

# Chart rendering (Playwright + lightweight-charts)
playwright>=1.40

# Optional: remote OpenAI-compatible embedding
openai>=1.50
